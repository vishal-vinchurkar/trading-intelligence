# Secrets — NEVER commit
.env
.env.*
!.env.example
!.env.local.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Backtest feature store — bulky and fully regenerable via `python -m data.backfill`.
# (The dashboard ships quant/scan.json + dashboard/data/scan.json instead.)
data/cache/

# Node / Next.js (dashboard, added in Phase 3)
node_modules/
.next/
out/
dist/
.vercel/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Forward paper-trading ledger — runtime state, accrues locally / via cron.
execution/ledger.json
logs/
execution/telegram_bridge_state.json
