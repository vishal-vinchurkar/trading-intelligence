# Browser-safe Supabase read credentials (anon key only — never the service role key).
# Read access is gated by the RLS policies in orchestrator/dashboard_policies.sql.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
